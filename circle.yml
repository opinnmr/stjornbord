machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t opinnmr/stjornbord .

test:
  override:
    - python manage.py test devices infoscreen register student user
